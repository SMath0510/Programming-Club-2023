/* Consider this as a formality in coding :: though this is really important */
#include<bits/stdc++.h>
using namespace std;

/* Your main starts here */
int main(){

    /* Declaration */
    int integer_variable;  // holds integer values
    float float_variable;  // holds decimal values
    bool boolean_variable; // holds true / false
    double double_variable; // holds decimal values (with higher precision)
    long double long_double_variable; // holds decimal values (with even higher precision)
    long long int long_long_integer_variable; // holds large integer vlaues
    char character_variable; // holds a character 
    string string_variable; // holds a string    

    /* Assignment */
    integer_variable = 1;
    float_variable = 1.5;
    character_variable = 'c'; // note its a single quote
    string_variable = "shaun";
    
    /* Declaring and Assigning */
    int num_int = 1;
    float num_float = 1.0;

    /* Declaring Multiple */
    int num1 = 1, num2 = 10; // with initialization
    float floatnum1, floatnum2;  // without initialization
    
}
